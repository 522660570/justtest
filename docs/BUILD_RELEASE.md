# 打包发布指南

## 🎉 功能已完成

一键续杯功能现已正常工作，可以打包发布了。

## 📦 打包命令

### 打包为便携版 exe（推荐）

```bash
npm run build-exe
```

生成文件：`dist-electron/Cursor Manager-1.0.0-portable.exe`

### 打包为安装包

```bash
npm run build-installer
```

生成文件：`dist-electron/Cursor Manager-1.0.0-installer.exe`

### 打包所有版本

```bash
npm run build-all
```

## 📂 打包输出

```
dist-electron/
├── Cursor Manager-1.0.0-portable.exe    ← 便携版（推荐）
├── Cursor Manager-1.0.0-installer.exe   ← 安装版
└── win-unpacked/                        ← 未打包版本（测试用）
```

## ⚙️ 打包配置

配置文件：`package.json`

```json
{
  "build": {
    "portable": {
      "requestExecutionLevel": "admin"  ← 以管理员权限运行
    },
    "win": {
      "requestedExecutionLevel": "requireAdministrator"  ← 需要管理员权限
    }
  }
}
```

## 🔑 关键设置

### 管理员权限

应用需要管理员权限才能修改 Cursor 的配置文件：
- storage.json
- state.vscdb
- machineId

### 文件包含

确保打包时包含：
- `dist/**/*` - 前端构建产物
- `electron/**/*` - Electron 主进程
- `node_modules/sqlite3/**/*` - SQLite 模块

## 🧪 测试打包后的应用

### 1. 打包

```bash
npm run build-exe
```

### 2. 运行打包后的应用

```
dist-electron/Cursor Manager-1.0.0-portable.exe
```

### 3. 测试功能

- 验证授权码
- 点击"一键续杯"
- 验证账号切换

### 4. 检查日志

如果出现问题，查看日志：
```
%USERPROFILE%\.cursor-renewal-tool\logs\
```

## 📝 发布清单

- [x] 功能测试通过
- [x] 关闭开发者工具
- [x] 清理调试文档
- [x] 清理测试文件
- [ ] 打包应用
- [ ] 测试打包后的应用
- [ ] 发布

## 🎯 下一步

1. 运行打包命令
2. 测试生成的 exe 文件
3. 如果一切正常，即可发布

---

**准备打包**: ✅  
**开发者工具**: 已关闭  
**调试文档**: 已清理




