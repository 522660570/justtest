spring:
  # 数据库配置 - 使用MySQL
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://111.228.33.54:3306/mycursor?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 520likobe
    driver-class-name: com.mysql.cj.jdbc.Driver
    
    # Druid连接池配置
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      
      # 监控配置
      web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*"
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        reset-enable: false
        login-username: admin
        login-password: 123456

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:mapper/**/*Mapper.xml
  type-aliases-package: com.mycursor.entity
        
  # 应用配置
  application:
    name: mycursor
    
        # 服务器配置
server:
  port: 8088
  servlet:
    context-path: /

# 应用自定义配置
app:
  cursor:
    # 是否使用真实API检测账号使用情况（如果为false则使用模拟数据）
    use-real-api: true
    # API请求超时时间（毫秒）
    api-timeout: 10000
  
  # 版本控制配置
  version:
    # 最新版本号（每次发布新版本时修改这里）
    latest: "1.2.1"
    # 更新标题
    update-title: "发现新版本"
    # 更新消息
    update-message: "检测到新版本，建议更新以获得更好的体验和最新功能！"
    # 下载地址
    download-url: "https://gitee.com/liweizhi66/cursor-free-tool/releases/tag/v1.2.1"
    # 是否强制更新（true=强制更新，false=可选更新）
    force-update: false
    # 更新内容列表
    features:
      - "可提升额度使用效率"

    
# 日志配置
logging:
  level:
    com.mycursor: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    
# 自定义配置
mycursor:
  license:
    default-days: 30
    max-devices: 1
    # 换号限制配置
    device-switch:
      enabled: true  # 是否启用换号次数限制（true=启用，false=禁用）
      max-daily-switches: 8  # 每天最多允许换号次数
      reset-time: "00:00:00"  # 每天重置时间（24小时制）
      min-switch-interval-minutes: 2  # 每次换号最小间隔时间（分钟）
  account:
    quota-check-interval: 3600 # 1小时检查一次额度
    accept-free-accounts: true # 是否接受 free 类型的账号（true=接受，false=不接受）
    check-quota-on-switch: false # 换号时是否检查账号额度（true=检查，false=不检查）
    use-free-accounts-for-switch: true # free 类型的账号是否可以用于一键换号（true=可以，false=不可以）
    # 🆕 getAccountByCode 接口控制开关
    enable-subscription-check: false  # 是否启用订阅状态检查（步骤3: 检查账号的订阅状态）
    enable-quota-check: false  # 是否启用额度检查（步骤4: 检查账号额度是否真的可用）

# Cursor Token 配置
cursor:
  token:
    # Token刷新服务器地址（用于从 WorkosCursorSessionToken 获取真正的 accessToken）
    # 参考 cursor-free-vip-main 项目的实现
    refresh-server: https://token.cursorpro.com.cn

# 安全配置
security:
  swagger:
    # Swagger访问认证配置
    # 重要：部署到生产环境时务必修改默认密码！
    username: admin
    password: 520likobe